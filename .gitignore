# ISO files (too large for git)
*.iso

# Secrets
autoinstall/.env
cert/

# Terraform (legacy)
.terraform/
.terraform.lock.hcl
tfplan
*.tfstate
*.tfstate.backup
*.tfvars.local
keys/
override.tf
override.tf.json
*_override.tf
*_override.tf.json
